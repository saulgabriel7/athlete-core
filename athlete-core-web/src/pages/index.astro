---
/**
 * ATHLETE CORE - Home Page
 * Página inicial com CTA para login
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/common/Button.astro';
import { SignedIn, SignedOut } from '@clerk/astro/components';
---

<BaseLayout title="ATHLETE CORE" description="Sistema de gerenciamento de treinos e alimentação">
  <main class="home">
    <div class="home-content">
      <div class="logo-container">
        <h1 class="logo">ATHLETE<span>CORE</span></h1>
      </div>
      
      <p class="tagline">
        Seu sistema completo de<br />
        <strong>treinos</strong> e <strong>alimentação</strong>
      </p>

      <div class="cta-buttons">
        <SignedIn>
          <Button href="/dashboard" variant="primary" size="lg">
            Acessar Dashboard
          </Button>
        </SignedIn>
        
        <SignedOut>
          <Button href="/sign-in" variant="primary" size="lg">
            Entrar
          </Button>
          <Button href="/sign-up" size="lg">
            Criar Conta
          </Button>
        </SignedOut>
      </div>

      <div class="features">
        <div class="feature">
          <span class="feature-icon">💪</span>
          <span class="feature-text">Treinos Personalizados</span>
        </div>
        <div class="feature">
          <span class="feature-icon">🥗</span>
          <span class="feature-text">Planos Alimentares</span>
        </div>
        <div class="feature">
          <span class="feature-icon">📊</span>
          <span class="feature-text">Acompanhamento</span>
        </div>
      </div>
    </div>

    <footer class="home-footer">
      <p>&copy; {new Date().getFullYear()} ATHLETE CORE</p>
    </footer>
  </main>
</BaseLayout>

<style>
  .home {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-xl);
    background: var(--color-white);
  }

  .home-content {
    text-align: center;
    max-width: 600px;
  }

  .logo-container {
    margin-bottom: var(--spacing-xl);
  }

  .logo {
    font-size: clamp(2.5rem, 10vw, 4rem);
    font-weight: var(--font-weight-bold);
    letter-spacing: -0.03em;
    text-transform: uppercase;
    margin: 0;
    line-height: 1;
  }

  .logo span {
    font-weight: var(--font-weight-light);
  }

  .tagline {
    font-size: var(--font-size-xl);
    color: var(--color-foreground-muted);
    margin-bottom: var(--spacing-2xl);
    line-height: 1.4;
  }

  .tagline strong {
    color: var(--color-foreground);
  }

  .cta-buttons {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    margin-bottom: var(--spacing-3xl);
  }

  .features {
    display: flex;
    justify-content: center;
    gap: var(--spacing-2xl);
    padding-top: var(--spacing-2xl);
    border-top: var(--border-width) solid var(--color-border);
  }

  .feature {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-sm);
  }

  .feature-icon {
    font-size: var(--font-size-2xl);
  }

  .feature-text {
    font-size: var(--font-size-sm);
    color: var(--color-foreground-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .home-footer {
    position: absolute;
    bottom: var(--spacing-lg);
  }

  .home-footer p {
    font-size: var(--font-size-sm);
    color: var(--color-foreground-muted);
    margin: 0;
  }

  @media (max-width: 768px) {
    .cta-buttons {
      flex-direction: column;
      width: 100%;
    }

    .features {
      flex-direction: column;
      gap: var(--spacing-lg);
    }
  }
</style>

